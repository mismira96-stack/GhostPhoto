<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FindGhostPhoto (Minimal Shell)"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="원칙: WRONG_MATCH = 0 (오선택 0건 절대 보장, 삭제 수동)"
        android:textColor="#555555"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="12dp">

        <Button
            android:id="@+id/btn_scan"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="로컬 스캔" />

        <Button
            android:id="@+id/btn_accessibility_status"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1.2"
            android:backgroundTint="#F1F3F4"
            android:textColor="#3C4043"
            android:text="접근성 설정" />
    </LinearLayout>

    <Button
        android:id="@+id/btn_find_and_select"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:backgroundTint="#1A73E8"
        android:textSize="14sp"
        android:textStyle="bold"
        android:text="🎯 [Google Photos에서 찾기] 소실 3건 탐색 &amp; 선택" />

    <Button
        android:id="@+id/btn_live_collect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:backgroundTint="#5F6368"
        android:text="🔍 P2-A: 구글포토 실시간 수집만 실행 (Read-Only)" />

    <Button
        android:id="@+id/btn_view_report"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:backgroundTint="#5F6368"
        android:text="📊 28건 Ground-Truth 대조 리포트 보기" />

    <TextView
        android:id="@+id/tv_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:background="#EEEEEE"
        android:padding="12dp"
        android:text="상태: 스캔 대기 중"
        android:textSize="13sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="소실 감지된 미디어 (Ghost Candidates):"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="💡 카드나 [구글포토 ↗]를 누르면 해당 날짜의 구글포토 검색창으로 바로 이동합니다."
        android:textColor="#1967D2"
        android:textSize="12sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_candidates"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_weight="1"
        tools:listitem="@layout/item_ghost_candidate" />

</LinearLayout>
